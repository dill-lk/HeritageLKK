# SUPABASE (client/Tauri-safe)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# SUPABASE (server-only)
# Use the same project URL/anon key values as VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY,
# but define them server-side so backend routes can read them.
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# AI provider keys (server-only fallback; never expose to client/Tauri)
NVIDIA_API_KEY=""
